# 搜索偏好设置

## 规则
每次搜索文件/代码时，优先使用**遍历目录**的方法：
1. 先用 `list_dir` 查看目录结构
2. 再用 `read_file` 读取具体文件内容

## 避免使用的工具
- `file_search` - 不优先使用
- `grep_search` - 不优先使用  
- `search tools` - 不优先使用

## 原因
用户明确要求"每次搜索时都使用遍历的方法"，这种方式更直观且与AE2源码学习风格一致。
